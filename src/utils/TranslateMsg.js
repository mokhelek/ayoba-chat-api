import TranslateMsgAPI from "./TranslateMsgAPI";

export default function TranslateMsg(message, langValue) {
    // Create a variable to store the translated messages
    // Use the TranslateMsgAPI function to translate the messages and store them in the translatedMessages variable
    TranslateMsgAPI(message, langValue);
}